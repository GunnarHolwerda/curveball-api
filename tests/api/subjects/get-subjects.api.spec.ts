
// TODO: Implement tests as soon as we have reliable DB backups of test subject date
xdescribe('GET /subjects', () => {
    beforeEach(() => {

    });

    it('should load only subjects relevant to topic and type specified', async () => {

    });

    it('should only load subjects relevant after the specified startDate', async () => {

    });

    it('should only load subjects relevant before the specified endDate', async () => {

    });

    it('should load subjects relevant between start and end date', async () => {

    });
});